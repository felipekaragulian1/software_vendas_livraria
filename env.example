# Configuração do Banco de Dados Legado (SQL Server)
# Copie este arquivo para .env.local e preencha os valores

# Host do SQL Server (apenas o IP ou hostname, SEM vírgula ou porta)
LEGACY_DB_HOST=51.222.51.77

# Porta do SQL Server (separada do host)
LEGACY_DB_PORT=7500

# Usuário do banco de dados
LEGACY_DB_USER=belinda

# Senha do banco de dados
LEGACY_DB_PASSWORD=sua_senha_aqui

# Nome do database
LEGACY_DB_NAME=Belinda

# Criptografia TLS (true ou false)
# Recomendado: true com trustServerCertificate=true (já configurado automaticamente)
LEGACY_DB_ENCRYPT=true

# IMPORTANTE:
# - NÃO coloque vírgula ou porta no LEGACY_DB_HOST (ex: "51.222.51.77,7500" está ERRADO)
# - Use apenas o host em LEGACY_DB_HOST e a porta em LEGACY_DB_PORT
# - O sistema usa trustServerCertificate=true automaticamente (equivalente ao .NET)
